#include "main.h"

/**
 * mul - multiplies two integers
 * @a: digit to multiply
 * @b: digit to multiply
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int c = 0;

	c = a * b;
	_putchar(c);

	return (c);

}
