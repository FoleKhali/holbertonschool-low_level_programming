#include "main.h"

/**
 * abs - compute the absolute value of an integer
 * @o: value
 * Always: Always 0.
 */
int _abs(int o)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
