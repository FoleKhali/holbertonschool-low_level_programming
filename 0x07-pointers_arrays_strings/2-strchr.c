
        unsigned int i;

        for (; ; i++)
        {
                if (s[i] == c)
                        return (&(s[i]));
                if (!s[i])
                        return (NULL);

        }
}
ed int i;

        for (; ; i++)
        {
                if (s[i] == c)
                        return (&(s[i]));
                if (!s[i])
                        return (NULL);

        }
}
